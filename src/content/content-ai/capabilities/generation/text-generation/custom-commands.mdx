---
title: 注册自定义命令和提示
meta:
  title: 自定义命令 | Tiptap 内容 AI
  description: 扩展 AI 生成扩展，以创建适用于您的 Tiptap 编辑器的自定义编辑器命令和提示。详见文档了解更多信息！
  category: 内容 AI
---

import { CodeDemo } from '@/components/CodeDemo'
import { Requirements, RequirementItem } from '@/components/Requirements'

<Requirements>
    <RequirementItem label="1. 激活试用或订阅">
        在您的账户中开始免费 [试用](https://cloud.tiptap.dev/v2/billing) 或订阅 [Tiptap 计划](https://cloud.tiptap.dev/v2/billing)。
    </RequirementItem>
    <RequirementItem label="2. 集成 AI 提供商">
        在您的 [AI 设置](https://cloud.tiptap.dev/v2/cloud/ai) 中配置 OpenAI 或查看 [自定义 LLM 指南](/content-ai/capabilities/generation/custom-llms)。
    </RequirementItem>
    <RequirementItem label="3. 从私有注册表安装">
        要安装前端扩展，请按照 [设置指南](/guides/pro-extensions) 的指示验证 Tiptap 的私有 npm 注册表。
    </RequirementItem>
</Requirements>

AI 生成扩展提供了一系列 [内置命令](/content-ai/capabilities/generation/text-generation/built-in-commands)，但您可以扩展它以定义自己的命令。这些自定义命令可用于使用您的自定义提示调用 LLM。

<CodeDemo isPro path="/Extensions/AiCustomCommandUsingAiTextPrompt" />

## 注册自定义命令

要注册您自己的 AI 命令，只需扩展 AI 生成扩展，在 `addCommands()` 中添加您的命令（不要忘记继承 `this.parent?.()` 中的预定义命令），然后执行 `aiTextPrompt()` 以运行您个人的提示。

请注意，此示例在客户端使用您的提示，这意味着用户可以查看它。如果您希望在后端使用自定义语言模型（LLM）或提示，请参考 [在这里提供的示例](/content-ai/custom-llms)。

```js
import { Ai, getHTMLContentBetween } from '@tiptap-pro/extension-ai'

// … 其他导入

// 如果使用 TypeScript，请声明扩展类型。
// 更多信息： https://tiptap.dev/docs/guides/typescript
//
// declare module '@tiptap/core' {
//   interface Commands<ReturnType> {
//     ai: {
//       aiCustomTextCommand: (options?: TextOptions) => ReturnType,
//     }
//   }
// }

const AiExtended = Ai.extend({
  addCommands() {
    return {
      ...this.parent?.(),

      aiCustomTextCommand:
        (options = {}) =>
        ({ editor, state }) => {
          const { from, to } = state.selection
          const selectedText = getHTMLContentBetween(editor, from, to)

          return editor.commands.aiTextPrompt({
            text: `将以下文本翻译成法语并添加一些表情符号：${selectedText}`,
            ...options,
          })
        },
    }
  },
})

// 初始化您的 Tiptap 编辑器实例并注册扩展

const editor = new Editor{
  extensions: [
    StarterKit,
    AiExtended.configure({
      /* … */
    }),
  ],
  content: '',
})

// 运行您自定义的命令
// editor.chain().focus().aiCustomTextCommand().run()
```