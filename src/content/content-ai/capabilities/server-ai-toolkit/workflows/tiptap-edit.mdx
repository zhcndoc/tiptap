---
title: Tiptap 编辑
meta:
  title: Tiptap 编辑 | Tiptap Server AI 工具包
  description: 为 Tiptap 文档构建通用的服务器端编辑工作流。
  category: 内容 AI
---

import { CodeDemo } from '@/components/CodeDemo'
import { Callout } from '@/components/ui/Callout'

构建一个通用的编辑工作流：读取文档、生成编辑操作，并将其应用到服务器上。

<CodeDemo path="" isScrollable src="https://ai-toolkit-demos.vercel.app/server-edit-workflow" />

查看 [GitHub 上的源代码](https://github.com/ueberdosis/ai-toolkit-demos)。

<Callout title="当 AI 需要重写现有内容时使用该工作流" variant="info">
  Tiptap 编辑工作流是最灵活的 Server AI Toolkit 工作流。可用于诸如
  重写段落、在目标节点附近插入新块，或转换整份文档等任务。
</Callout>

开始之前，请先按照 [授权指南](/content-ai/capabilities/server-ai-toolkit/install#set-up-authorization) 完成身份验证设置。

## 1. 从 AI Server 读取文档

读取你希望模型处理的文档部分。调用：

- `POST /v3/ai/toolkit/read/read-document`

```ts
import { getAuthHeaders } from '@/lib/server-ai-toolkit/get-auth-headers'

const readResponse = await fetch(`${apiBaseUrl}/toolkit/read/read-document`, {
  method: 'POST',
  headers: getAuthHeaders(),
  body: JSON.stringify({
    editorContext,
    format: 'shorthand',
    reviewOptions: {
      mode: 'disabled',
    },
    experimental_documentOptions: {
      documentId,
      userId: 'ai-assistant',
    },
  }),
})

const readResult = await readResponse.json()
```

## 2. 生成编辑操作

从 AI Server 获取工作流定义，并让模型生成结构化的编辑操作。

```ts
const workflowResponse = await fetch(`${apiBaseUrl}/toolkit/workflows/edit`, {
  method: 'POST',
  headers: getAuthHeaders(),
  body: JSON.stringify({
    format: 'shorthand',
  }),
})

const workflow = await workflowResponse.json()

const toolkitResponse = await fetch(`${apiBaseUrl}/toolkit/tools`, {
  method: 'POST',
  headers: getAuthHeaders(),
  body: JSON.stringify({
    editorContext,
  }),
})

const { prompt: toolkitPrompt } = await toolkitResponse.json()

const result = streamText({
  model,
  system: `${workflow.systemPrompt}\n\n${toolkitPrompt}`,
  prompt: JSON.stringify({
    content: readResult.output.content,
    task,
  }),
  output: Output.object({ schema: z.fromJSONSchema(workflow.outputSchema) }),
})

const output = await result.output
```

## 3. 执行编辑工作流

将生成的操作发送回 Server AI Toolkit。当你使用的是 Tiptap Cloud 文档时，服务器会自动更新协作文档。调用：

- `POST /v3/ai/toolkit/execute-workflow/tiptap-edit`

```ts
const executeResponse = await fetch(`${apiBaseUrl}/toolkit/execute-workflow/tiptap-edit`, {
  method: 'POST',
  headers: getAuthHeaders(),
  body: JSON.stringify({
    editorContext,
    format: 'shorthand',
    input: output,
    reviewOptions: {
      mode: 'disabled',
    },
    experimental_documentOptions: {
      documentId,
      userId: 'ai-assistant',
    },
  }),
})

const executeResult = await executeResponse.json()
```

## 4. 从编辑器 UI 触发工作流

```tsx
const response = await fetch('/api/server-edit-workflow', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    documentId,
    editorContext: getEditorContext(editor),
    task,
  }),
})
```

## 跟踪更改（Tracked changes）

将此工作流与 [Tracked Changes](/tracked-changes/getting-started/overview) 扩展集成，以便在 AI 编辑文档后显示审阅界面，并允许用户接受和拒绝更改。

在向 `POST /v3/ai/toolkit/execute-workflow/tiptap-edit` 发起的请求中，配置 `reviewOptions` 参数：

```ts
reviewOptions: {
  mode: 'trackedChanges',
  trackedChangesOptions: {
    userId: 'ai-assistant',
  },
}
```

<CodeDemo
  path=""
  isScrollable
  src="https://ai-toolkit-demos.vercel.app/server-edit-workflow-tracked-changes"
/>

查看 [AI Toolkit 演示](https://github.com/ueberdosis/ai-toolkit-demos)，了解如何将 Server AI Toolkit 工作流与 Tracked Changes 集成的示例。

## 带评论的 Tracked changes

<CodeDemo
  isLarge
  path=""
  isScrollable
  src="https://ai-toolkit-demos.vercel.app/server-edit-workflow-tracked-comments"
/>

查看 [GitHub 上的源代码](https://github.com/ueberdosis/ai-toolkit-demos)。

你可以让 AI 为每项更改提供理由。每条理由都会变成一个与其跟踪更改关联的评论线程，使用 [Comments](/comments/getting-started/overview) 扩展实现。

### 带有 `operationMeta` 的工作流定义

获取编辑工作流定义时，传入 `operationMeta`，以要求每个操作都包含一个 `meta` 字段。你提供的字符串用于描述 `meta` 字段中应包含的内容。

```ts
const workflowResponse = await fetch(`${apiBaseUrl}/toolkit/workflows/edit`, {
  method: 'POST',
  headers: getAuthHeaders(),
  body: JSON.stringify({
    format: 'shorthand',
    operationMeta: 'Specific reason for this edit (what is changing in this exact operation).',
  }),
})
```

返回的 `outputSchema` 现在要求每个操作都包含一个 `meta` 字段，而 `systemPrompt` 会指示 AI 填充它。

### 使用评论执行工作流

执行工作流时，除了 `reviewOptions` 之外，还要传入 `experimental_commentsOptions`：

```ts
const executeResponse = await fetch(`${apiBaseUrl}/toolkit/execute-workflow/tiptap-edit`, {
  method: 'POST',
  headers: getAuthHeaders(),
  body: JSON.stringify({
    editorContext,
    format: 'shorthand',
    input: output,
    reviewOptions: {
      mode: 'trackedChanges',
      trackedChangesOptions: {
        userId: 'ai-assistant',
      },
    },
    experimental_commentsOptions: {
      threadData: { userName: 'Tiptap AI' },
      commentData: { userName: 'Tiptap AI' },
    },
    experimental_documentOptions: {
      documentId,
      userId: 'ai-assistant',
    },
  }),
})
```

操作中每个非空的 `meta` 字段都会变成一个与其跟踪更改关联的评论线程。理由会作为该线程的第一条评论内容保存，并作为线程数据中的 `suggestionReason` 保存。

### 客户端设置

在客户端中，结合 `TiptapCollabProvider` 添加 `CommentsKit` 扩展：

```tsx
import { CommentsKit } from '@tiptap-pro/extension-comments'
import { TrackedChanges } from '@tiptap-pro/extension-tracked-changes'
import { ServerAiToolkit } from '@tiptap/server-ai-toolkit'

const editor = useEditor({
  extensions: [
    StarterKit.configure({ undoRedo: false }),
    Collaboration.configure({ document: doc }),
    TrackedChanges.configure({ enabled: false }),
    ServerAiToolkit,
    CommentsKit.configure({
      provider, // 你的 TiptapCollabProvider 实例
    }),
  ],
})
```

用户可以在评论侧边栏中审阅每个跟踪更改，并阅读 AI 的理由说明。

## 最终效果

完成的演示会在服务器端将协作文档进行整体重写：

<CodeDemo path="" isScrollable src="https://ai-toolkit-demos.vercel.app/server-edit-workflow" />

查看 [GitHub 上的源代码](https://github.com/ueberdosis/ai-toolkit-demos)。

## 下一步

- 查看 [Server AI Toolkit REST API 参考](/content-ai/capabilities/server-ai-toolkit/api-reference/rest-api)
- 查看 [REST API 请求详情](/content-ai/capabilities/server-ai-toolkit/api-reference/rest-api#execute-the-tiptap-edit-workflow)
