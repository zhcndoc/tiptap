---
title: exitCode 命令
meta:
  title: exitCode 命令 | Tiptap 编辑器文档
  description: 在 Tiptap 中使用 exitCode 命令以退出代码块并在新的默认块中继续编辑。更多详情请查阅我们的文档！
  category: 编辑器
---

`exitCode` 命令将在当前选区后创建一个默认块，如果选区是 `code` 元素，并将光标移动到新块中。

## 使用 exitCode 命令

```js
editor.commands.exitCode()
```