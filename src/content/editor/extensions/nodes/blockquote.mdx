---
title: 引用块扩展
tags:
  - type: image
    src: https://img.shields.io/npm/v/@tiptap/extension-blockquote/next?label=版本
    url: https://www.npmjs.com/package/@tiptap/extension-blockquote
    label: 版本
  - type: image
    src: https://img.shields.io/npm/dm/@tiptap/extension-blockquote.svg
    url: https://npmcharts.com/compare/@tiptap/extension-blockquote?minimal=true
    label: 下载量
extension:
  name: 引用块
  link: https://github.com/ueberdosis/tiptap/tree/main/packages/extension-blockquote
  description: 引用他人内容会让你看起来更聪明。
  type: node
  icon: TextQuote
meta:
  title: 引用块扩展 | Tiptap 编辑器文档
  description: 在 Tiptap 中使用引用块扩展，启用编辑器中的 `<blockquote>` HTML 标签。详细信息请查阅我们的文档！
  category: 编辑器
---

import { CodeDemo } from '@/components/CodeDemo'

引用块扩展允许你在编辑器中使用 `<blockquote>` HTML 标签。这对于……展示引用内容非常有用，对吧？

在新行开头输入 `<` 之后跟着 `&gt; `，就会神奇地变成引用块。

<CodeDemo path="/Nodes/Blockquote" />

## 安装

```bash
npm install @tiptap/extension-blockquote@beta
```

## 配置

### HTMLAttributes

自定义要添加到渲染HTML标签中的HTML属性。

```js
Blockquote.configure({
  HTMLAttributes: {
    class: '我的自定义类',
  },
})
```

## 命令

### setBlockquote()

将内容包裹在引用块中。

```js
editor.commands.setBlockquote()
```

### toggleBlockquote()

切换引用块的显示与隐藏（包裹或解包引用块）。

```js
editor.commands.toggleBlockquote()
```

### unsetBlockquote()

移除引用块包裹。

```js
editor.commands.unsetBlockquote()
```

## 键盘快捷键

| 命令             | Windows/Linux                                            | macOS                                               |
| ---------------- | -------------------------------------------------------- | --------------------------------------------------- |
| 切换引用块       | <kbd>Control</kbd> + <kbd>Shift</kbd> + <kbd>B</kbd>   | <kbd>Cmd</kbd> + <kbd>Shift</kbd> + <kbd>B</kbd>    |

## 源码

[packages/extension-blockquote/](https://github.com/ueberdosis/tiptap/blob/main/packages/extension-blockquote/)