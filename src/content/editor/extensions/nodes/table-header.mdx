---
title: 表头扩展
tags:
  - type: image
    src: https://img.shields.io/npm/v/@tiptap/extension-table/next?label=版本
    url: https://www.npmjs.com/package/@tiptap/extension-table
    label: 版本
  - type: image
    src: https://img.shields.io/npm/dm/@tiptap/extension-table.svg
    url: https://npmcharts.com/compare/@tiptap/extension-table?minimal=true
    label: 下载量
meta:
  title: 表头扩展 | Tiptap 编辑器文档
  description: 使用 Tiptap 的表头扩展改进表格。轻松控制表格头部。文档中的源代码和使用示例！
  category: 编辑器
extension:
  name: 表头
  link: https://github.com/ueberdosis/tiptap/tree/main/packages/extension-table
  description: '表格不需要头部，但说实话：有头部看起来更好。'
  type: node
  icon: 表格
---

import { CodeDemo } from '@/components/CodeDemo'

该扩展补充了 [`Table`](/editor/extensions/nodes/table) 扩展，并添加了… 你猜对了… 表格头部。

<CodeDemo path="/Nodes/Table" />

## 安装

```bash
npm install @tiptap/extension-table@beta
```

此扩展需要安装 [`Table`](/editor/extensions/nodes/table) 扩展。

此扩展随 `TableKit` 扩展默认安装，因此您无需单独安装它。

```ts
import { Editor } from '@tiptap/core'
import { TableKit } from '@tiptap/extension-table'

new Editor({
  extensions: [TableKit],
})
```

## 使用

表头是可选的。但来吧，你想要它们，对吧？如果你不想要它们，可以像这样更新 [`TableRow`](/editor/extensions/nodes/table-row) 扩展的 `content` 属性：

```js
// 不带表头的表格行
TableRow.extend({
  content: 'tableCell*',
})
```

这是默认设置，允许表头：

```js
// 带表头的表格行（默认）
TableRow.extend({
  content: '(tableCell | tableHeader)*',
})
```

## 源代码

[packages/extension-table-header/](https://github.com/ueberdosis/tiptap/blob/main/packages/extension-table/src/header)

## 最小安装

```js
import { Editor } from '@tiptap/core'
import { TableHeader } from '@tiptap/extension-table/header'

new Editor({
  extensions: [TableHeader],
})
```