---
title: 详情扩展
icon: menu-unfold-line
extension:
  name: Details
  description: 在编辑器内容中添加可折叠的详情节点。
  link: https://github.com/ueberdosis/tiptap/tree/main/packages/extension-details
  type: node
  icon: ListCollapse
tags:
  - type: image
    src: https://img.shields.io/npm/v/@tiptap/extension-details.svg?label=version
    url: https://www.npmjs.com/package/@tiptap/extension-details
    label: 版本
  - type: image
    src: https://img.shields.io/npm/dm/@tiptap/extension-details.svg
    url: https://npmcharts.com/compare/@tiptap/extension-details?minimal=true
    label: 下载量
meta:
  title: 详情扩展 | Tiptap 编辑器文档
  description: 在 Tiptap 中使用详情扩展以启用 `<details>` HTML 标签用于显示和隐藏内容。更多内容请参阅我们的文档！
  category: 编辑器
---

import { CodeDemo } from '@/components/CodeDemo'
import { Callout } from '@/components/ui/Callout'

详情扩展使您能够在编辑器中使用 `<details>` HTML 标签。这非常适合显示和隐藏内容。

<CodeDemo path="/Nodes/Details" />

## 安装

```bash
npm install @tiptap/extension-details@beta
```

此扩展要求 [`DetailsSummary`](/editor/extensions/nodes/details-summary) 和 [`DetailsContent`](/editor/extensions/nodes/details-content) 节点。

## 设置

### persist

指定打开状态是否应保存在文档中。默认为 `false`。

```js
Details.configure({
  persist: true,
})
```

### openClassName

指定切换内容时设置的 CSS 类。默认为 `is-open`。

```js
Details.configure({
  openClassName: 'is-open',
})
```

### HTMLAttributes

应添加到渲染的 HTML 标签中的自定义 HTML 属性。

```js
Details.configure({
  HTMLAttributes: {
    class: 'my-custom-class',
  },
})
```

## 命令

### setDetails()

将内容包装在一个详情节点中。

```js
editor.commands.setDetails()
```

### unsetDetails()

解包详情节点。

```js
editor.commands.unsetDetails()
```