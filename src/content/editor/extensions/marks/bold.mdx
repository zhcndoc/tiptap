---
title: 粗体扩展
tags:
  - type: image
    src: https://img.shields.io/npm/v/@tiptap/extension-bold/next?label=version
    url: https://www.npmjs.com/package/@tiptap/extension-bold
    label: 版本
  - type: image
    src: https://img.shields.io/npm/dm/@tiptap/extension-bold.svg
    url: https://npmcharts.com/compare/@tiptap/extension-bold?minimal=true
    label: 下载量
extension:
  name: 粗体
  link: https://github.com/ueberdosis/tiptap/tree/main/packages/extension-bold
  description: '让你的文字加粗，突出显示。'
  type: mark
  icon: Bold
meta:
  title: 粗体扩展 | Tiptap 编辑器文档
  description: 在 Tiptap 中使用粗体扩展让你的文字加粗并突出显示。在我们的文档中查看更多信息！
  category: 编辑器
---

import { Callout } from '@/components/ui/Callout'
import { CodeDemo } from '@/components/CodeDemo'

使用此扩展以**加粗**文本。如果你传入 `<strong>`、`<b>` 标签，或编辑器初始内容中带有设置 `font-weight` CSS 规则的内联 `style` 属性的文本，它们都会被相应地渲染。

输入 `**两个星号**` 或 `__两个下划线__`，在输入时它们会神奇地转换为**加粗**文本。

<CodeDemo path="/Marks/Bold" />

<Callout title="限制" variant="default">
    当读取 `Editor` 实例的内容时，该扩展将生成相应的 `<strong>` HTML 标签。所有标记为粗体的文本，无论使用何种方法，都会被规范化为 `<strong>` HTML 标签。
</Callout>

## 安装

```bash
npm install @tiptap/extension-bold@next
```

## 设置

### HTMLAttributes

自定义应添加到渲染 HTML 标签的 HTML 属性。

```js
Bold.configure({
  HTMLAttributes: {
    class: 'my-custom-class',
  },
})
```

## 命令

### setBold()

将文本标记为粗体。

```js
editor.commands.setBold()
```

### toggleBold()

切换粗体标记。

```js
editor.commands.toggleBold()
```

### unsetBold()

移除粗体标记。

```js
editor.commands.unsetBold()
```

## 键盘快捷键

| 命令          | Windows/Linux                    | macOS                        |
| ------------ | -------------------------------- | ----------------------------- |
| toggleBold() | <kbd>Control</kbd> + <kbd>B</kbd> | <kbd>Cmd</kbd> + <kbd>B</kbd> |

## 源代码

[packages/extension-bold/](https://github.com/ueberdosis/tiptap/blob/main/packages/extension-bold/)