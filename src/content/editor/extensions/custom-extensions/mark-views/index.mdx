---
title: 创建自定义标记视图
meta:
  description: 在你的 Tiptap 编辑器中自定义和创建标记视图，享受交互式标记视图。更多内容见文档！
  title: 标记视图 | Tiptap 编辑器文档
  category: 编辑器
---

import { Callout } from '@/components/ui/Callout'

虽然 [节点视图](/editor/extensions/custom-extensions/node-views) 用于渲染节点，但标记视图用于渲染标记。它们相对简单，因为它们不必处理节点的复杂性，并且提供了更简单的 API。

标记视图极大地改善了编辑器内的体验，但也可以在 Tiptap 的只读实例中使用。它们设计上与 HTML 输出无关，因此你可以完全控制编辑器内的体验 _和_ 输出。

### 渲染 JavaScript/Vue/React

但是如果你想渲染你的实际 JavaScript/Vue/React 代码呢？使用 [静态渲染器](/editor/api/utilities/static-renderer)。这个工具可以让你将内容渲染为 HTML、Markdown 或 React 组件，而无需 Editor 实例。
