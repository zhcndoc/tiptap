---
title: PDF导出中的分页符
tags:
  - type: start
  - type: beta
meta:
  title: PDF导出中的分页符 | Tiptap转换
  description: 了解在将Tiptap编辑器内容导出为PDF文件时的分页符支持。
  category: 转换
---

import { Requirements, RequirementItem } from '@/components/Requirements'
import { EnterpriseCalloutAuto } from '@/components/EnterpriseCalloutAuto'

<EnterpriseCalloutAuto variant="pdf" renderMode="sidebar" disableWaitlist />

<Requirements>
    <RequirementItem label="1. 激活试用或订阅">
        在你的账户中开始[免费试用](https://cloud.tiptap.dev/v2?trial=true)或[订阅 Starter 计划](https://cloud.tiptap.dev/v2/billing)。
    </RequirementItem>
    <RequirementItem label="2. 从私有仓库安装">
        要安装此前端扩展，请按照[设置指南](/guides/pro-extensions)向 Tiptap 的私有 npm 仓库进行身份验证。
    </RequirementItem>
</Requirements>

[PageBreak 扩展](/pages/core-concepts/page-break-node)让用户可以在编辑器中插入显式的分页符。当与 PDF 导出扩展结合使用时，这些分页符会包含在导出的文档中。

## 工作原理

PDF 导出完全支持分页符。PDF 导出在底层使用 DOCX 转换，其中 `pageBreak` 节点会被转换为 `<w:br w:type="page"/>`，从而在生成的 PDF 中产生标准的分页符。

## 设置 PageBreak 与 PDF 导出

将 PageBreak 扩展与 PDF 导出扩展一起注册。编辑器内容中的分页符将在导出时自动转换。

```js
import { Editor } from '@tiptap/core'
import StarterKit from '@tiptap/starter-kit'
import { ExportPdf } from '@tiptap-pro/extension-export-pdf'
import { PageBreak } from '@tiptap-pro/extension-pagebreak'

const editor = new Editor({
  extensions: [
    StarterKit,
    PageBreak,
    ExportPdf.configure({
      appId: 'your-app-id',
      token: 'your-jwt',
      onCompleteExport: (result) => {
        const blob = new Blob([result], { type: 'application/pdf' })
        const url = URL.createObjectURL(blob)
        const a = document.createElement('a')
        a.href = url
        a.download = 'export.pdf'
        a.click()
        URL.revokeObjectURL(url)
      },
    }),
  ],
})
```

## 了解更多

- [PageBreak 扩展参考](/pages/core-concepts/page-break-node)了解完整的选项、命令和键盘快捷键
- [DOCX 导入中的分页符](/conversion/import/docx/page-breaks)了解导入支持
- [PDF 导出指南](/conversion/export/pdf/editor-export)了解常规导出设置
