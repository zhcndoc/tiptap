---
title: 导出自定义样式到 PDF
tags:
  - type: start
  - type: beta
  - type: version
    label: v0.3.0
meta:
    title: 将样式导出为 PDF | Tiptap 转换
    description: 学习如何使用 Export PDF 扩展将 Tiptap JSON 的自定义样式导出为 PDF。
    category: Conversion
---

import { Requirements, RequirementItem } from '@/components/Requirements'
import { EnterpriseCalloutAuto } from '@/components/EnterpriseCalloutAuto'

<EnterpriseCalloutAuto variant="pdf" renderMode="sidebar" disableWaitlist />

<Requirements>
    <RequirementItem label="1. 激活试用或订阅">
        在您的账户中开始[免费试用](https://cloud.tiptap.dev/v2?trial=true)或[订阅起始套餐](https://cloud.tiptap.dev/v2/billing)。
    </RequirementItem>
    <RequirementItem label="2. 从私有仓库安装">
        要安装此前端扩展，请按照[设置指南](/guides/pro-extensions)登录 Tiptap 私有 npm 仓库进行身份验证。
    </RequirementItem>
</Requirements>

导出为 PDF 时，可以定义自定义样式覆盖，这些样式将应用于导出的文档。当你想要定制导出内容的外观，超出默认样式时，这非常有用。

## 样式覆盖

`styleOverrides` 选项接受一个 `Record<string, string>` 对象，键为样式属性名，值为样式属性值。这些覆盖样式会发送至 Tiptap 转换服务，并在转换过程中生效。

| 参数             | 类型                     | 说明                                                      | 默认值       |
| ---------------- | ------------------------ | --------------------------------------------------------- | ------------ |
| `styleOverrides` | `Record<string, string>` | 应用于导出 PDF 文档的样式覆盖                             | `undefined`  |

## 示例

```js
import { ExportPdf } from '@tiptap-pro/extension-export-pdf'

const editor = new Editor({
  extensions: [
    // other extensions...
    ExportPdf.configure({
      token: 'YOUR_TOKEN',
      appId: 'YOUR_APP_ID',
      styleOverrides: {
        'font-family': 'Arial, sans-serif',
        'font-size': '12pt',
        'line-height': '1.5',
        'color': '#333333',
      },
    }),
    // other extensions...
  ],
})

// Export using custom styles
editor
  .chain()
  .exportPdf({
    onCompleteExport(result) {
      const url = URL.createObjectURL(result)
      const a = document.createElement('a')

      a.href = url
      a.download = 'styled-document.pdf'
      a.click()

      URL.revokeObjectURL(url)
    },
  })
  .run()
```
