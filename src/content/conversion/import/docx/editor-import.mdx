---
title: 在编辑器中导入 .docx
tags:
  - type: start
  - type: beta
  - type: version
    label: v0.7.0
meta:
    title: 导入 DOCX | Tiptap 转换
    description: 学习如何使用我们的文档中的导入扩展将 DOCX（Word）文档导入 Tiptap 编辑器。
    category: 转换
---

import { Callout } from '@/components/ui/Callout'
import { CodeDemo } from '@/components/CodeDemo'
import { Requirements, RequirementItem } from '@/components/Requirements'
import { EnterpriseCalloutAuto } from '@/components/EnterpriseCalloutAuto'

<EnterpriseCalloutAuto variant="docx" renderMode="sidebar" disableWaitlist />

<Requirements>
    <RequirementItem label="1. 激活试用或订阅">
        在你的账户中开始一个[免费试用](https://cloud.tiptap.dev/v2?trial=true)或[订阅起始计划](https://cloud.tiptap.dev/v2/billing)。
    </RequirementItem>
    <RequirementItem label="2. 从私有注册表安装">
        若要安装此前端扩展，请按照[设置指南](/guides/pro-extensions)中的步骤验证 Tiptap 的私有 npm 注册表。
    </RequirementItem>
</Requirements>

<CodeDemo src="https://feature-beta-conversion-extensions--tiptap-pro.netlify.app/preview/Extensions/ImportDocx" />

将 `.docx` 文件转换为 Tiptap JSON 使用 `@tiptap-pro/extension-import-docx` 编辑器扩展很简单，该扩展可以直接集成到你的 Tiptap 编辑器中。

如果你需要在编辑器外导入 `.docx` 内容，请使用[REST API](/conversion/import/docx/rest-api)。

## 安装 DOCX 导入扩展

转换扩展发布在 Tiptap 的私有 npm 注册表中。按照[私有注册表指南](/guides/pro-extensions)集成扩展。

安装 Tiptap 导入扩展包：

```bash
npm i @tiptap-pro/extension-import-docx
```

确保你的编辑器包含处理 DOCX 内容所需的所有 Tiptap 扩展。例如，包含图像扩展以处理行内图像，和表格扩展以处理表格。

## 所需扩展
为了完全映射 DOCX 内容（例如图像、表格、格式化文本）到 Tiptap 的架构中，必须包含相关的 Tiptap 扩展。没有这些扩展，某些 DOCX 元素可能不会被编辑器识别或正确呈现。

```js
import StarterKit from '@tiptap/starter-kit'
import Color from '@tiptap/extension-color'
import FontFamily from '@tiptap/extension-font-family'
import Highlight from '@tiptap/extension-highlight'
import { Image } from '@tiptap/extension-image'
import Paragraph from '@tiptap/extension-paragraph'
import { TableKit } from '@tiptap/extension-table'
import TextAlign from '@tiptap/extension-text-align'
import { TextStyle } from '@tiptap/extension-text-style'
```

## 配置
将导入扩展添加到你的编辑器设置中。

```ts
// Import the import extension
import { Import } from '@tiptap-pro/extension-import-docx'

const editor = new Editor({
  extensions: [
    // 其他扩展 ...
    Import.configure({
      appId: 'your-app-id', // 你的 Convert 应用 ID（请参阅 Tiptap Cloud 设置）
      token: 'your-jwt', // 用于身份验证的 JWT（请参阅身份验证文档）
      imageUploadCallbackUrl: 'https://your-image-upload-endpoint.com', // 你上传图像的端点
    }),
    // 其他扩展 ...
  ],
  // 其他编辑器设置 ...
})
```

| 属性 | 描述 |
|--------|-------------|
| `appId` | 你的 Tiptap 转换应用的 ID（在你的 Tiptap 账户的[转换设置](https://cloud.tiptap.dev/convert-settings)中查找） |
| `token` | 由你的服务器为转换服务生成的 JWT 身份验证令牌。（有关获取和使用这些凭据的详细信息，请参见[身份验证指南](https://cloud.tiptap.dev/convert-settings)。） |
| `imageUploadCallbackUrl` | 上传 DOCX 中找到的图像的端点。转换服务将把每个嵌入图像发送到此 URL，并在内容中使用返回的 URL。无法处理的任何图像将被删除。 |

## 导入 DOCX 文件
扩展配置完成后，可以导入用户选择的 DOCX 文件。

### 基本导入
最简单的方法是将文件直接传递给 `import` 命令。在这里，它用转换的内容替换当前的编辑器内容并聚焦编辑器：
```js
editor.chain().focus().import({ file }).run()
```
在大多数情况下，这一行代码就足以让用户导入 `.docx` 文件。扩展负责将文件发送到转换端点、检索转换后的 Tiptap JSON，然后将其插入到编辑器中。

### 导入处理
为了在导入过程完成后获得更多控制，可以使用 `onImport` 回调来处理转换结果。此回调提供转换后的内容、发生的任何错误以及一个名为 `setEditorContent` 的函数来将 `context.content` 中的内容插入到编辑器中。如果不提供 `onImport` 回调，扩展将自动将内容插入到编辑器中，但你将无法处理任何其他内容，如错误或加载状态。

```js
editor
  .chain()
  .import({
    file,
    onImport(context) {
      const { setEditorContent, content, error } = context

      // 添加错误处理
      if (error) {
        showErrorToast({ message: error.message })
      }

      // 在插入之前，你也可以修改内容
      content.doc.content.push({ type: 'paragraph', content: [{ type: 'text', text: '你好!' }] })

      // 例如，你可以更改应用的加载状态
      isLoading = false

      // 在编辑器中执行插入
      editor.commands.setEditorContent(content)
    },
  })
  .focus()
  .run()
```

我们在上面的例子中控制的操作：

| 操作 | 描述 |
|----------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 错误处理       | 如果转换失败，可以显示一个提示或记录错误。                                                                                                                                                                                                                  |
| 内容修改 | 可以插入额外的节点、删除某些节点，或根据需要调整转换后的 Tiptap JSON。                                                                                                                                                                          |
| 编辑器插入     | 如果希望依赖扩展的默认插入行为（替换编辑器内容），可以调用回调中提供的 `setEditorContent()` 函数。如果你自己修改内容，必须手动使用 `editor.commands.setContent(content)` 设置它。 |

## 页眉和页脚

当导入包含页眉和页脚的 `.docx` 文件时，如果已安装[页面扩展](/editor/extensions/functionality/pages)，导入扩展会自动检测并应用它们。

### 自动处理

如果在编辑器中注册了页面扩展，则在调用 `setEditorContent()` 时，会自动应用页眉和页脚：

```js
editor
  .chain()
  .import({
    file,
    onImport(context) {
      if (context.error) {
        console.error(context.error)
        return
      }

      // 页眉和页脚会和主体内容一起自动应用
      context.setEditorContent()
    },
  })
  .focus()
  .run()
```

如果页面扩展未安装，页眉和页脚数据将被静默忽略，正文内容按常规导入。

### 手动处理

`onImport` 回调提供所有页眉和页脚字段，供手动处理：

```js
editor
  .chain()
  .import({
    file,
    onImport(context) {
      if (context.error) {
        console.error(context.error)
        return
      }

      // 直接访问页眉/页脚数据
      const {
        header,           // 默认页眉（Tiptap JSON 或 null）
        footer,           // 默认页脚（Tiptap JSON 或 null）
        headerFirstPage,  // 首页页眉（Word 中启用“首页不同”时）（Tiptap JSON 或 null）
        footerFirstPage,  // 首页页脚（Word 中启用“首页不同”时）（Tiptap JSON 或 null）
        headerOdd,        // 奇数页页眉（Word 中启用“奇偶页不同”时）（Tiptap JSON 或 null）
        footerOdd,        // 奇数页页脚（Word 中启用“奇偶页不同”时）（Tiptap JSON 或 null）
        headerEven,       // 偶数页页眉（Word 中启用“奇偶页不同”时）（Tiptap JSON 或 null）
        footerEven,       // 偶数页页脚（Word 中启用“奇偶页不同”时）（Tiptap JSON 或 null）
      } = context

      // 设置正文内容，不自动应用页眉/页脚
      editor.commands.setContent(context.content)

      // 通过页面扩展命令手动应用页眉和页脚
      if (header) editor.commands.setHeader(header)
      if (footer) editor.commands.setFooter(footer)

      if (headerFirstPage || footerFirstPage) {
        editor.commands.setDifferentFirstPage(true)
        if (headerFirstPage) editor.commands.setHeaderFirstPage(headerFirstPage)
        if (footerFirstPage) editor.commands.setFooterFirstPage(footerFirstPage)
      }

      if (headerOdd || headerEven || footerOdd || footerEven) {
        editor.commands.setDifferentOddEven(true)
        if (headerOdd) editor.commands.setHeaderOdd(headerOdd)
        if (headerEven) editor.commands.setHeaderEven(headerEven)
        if (footerOdd) editor.commands.setFooterOdd(footerOdd)
        if (footerEven) editor.commands.setFooterEven(footerEven)
      }
    },
  })
  .focus()
  .run()
```

### 可用字段

| 字段 | 描述 |
|-------|-------------|
| `header` | 默认页眉内容（Tiptap JSON），或 `null` |
| `footer` | 默认页脚内容（Tiptap JSON），或 `null` |
| `headerFirstPage` | 首页页眉（Word 中启用“首页不同”时），或 `null` |
| `footerFirstPage` | 首页页脚（Word 中启用“首页不同”时），或 `null` |
| `headerOdd` | 奇数页页眉（Word 中启用“奇偶页不同”时），或 `null` |
| `footerOdd` | 奇数页页脚（Word 中启用“奇偶页不同”时），或 `null` |
| `headerEven` | 偶数页页眉（Word 中启用“奇偶页不同”时），或 `null` |
| `footerEven` | 偶数页页脚（Word 中启用“奇偶页不同”时），或 `null` |

## 脚注和尾注

当导入包含脚注或尾注的 `.docx` 文件时，导入扩展会在 `onImport` 回调中返回它们与文档内容一起。

### 访问脚注和尾注数据

`onImport` 回调提供 `footnotes` 和 `endnotes` 字段以供处理：

```js
editor
  .chain()
  .import({
    file,
    onImport(context) {
      if (context.error) {
        console.error(context.error)
        return
      }

      // 访问脚注和尾注数据
      const { footnotes, endnotes } = context

      // 脚注和尾注是以注释 ID 为键的对象
      // 每个值是一个 Tiptap JSON 文档: { type: "doc", content: [...] }
      console.log('脚注:', footnotes)
      console.log('尾注:', endnotes)

      // 插入主要内容到编辑器
      context.setEditorContent()
    },
  })
  .focus()
  .run()
```

### 可用字段

| 字段 | 描述 |
|-------|-------------|
| `footnotes` | 以脚注 ID 为键的对象，每个值为 Tiptap JSON 文档，若文档无脚注则为 `{}` |
| `endnotes` | 以尾注 ID 为键的对象，每个值为 Tiptap JSON 文档，若文档无尾注则为 `{}` |

正文中的内联引用表示为 `footnoteReference` 和 `endnoteReference` 节点，每个节点带有一个 `noteId` 属性，指向对应的注释条目。

## 详细输出

`verbose` 属性是一个位掩码（bitmask）数字，用于决定输出哪些类型的日志消息。扩展使用以下级别：

| 值 | 级别   | 描述                |
|-------|---------|----------------------------|
| 1     | `log`   | 一般信息日志 |
| 2     | `warn`  | 警告信息                   |
| 4     | `error` | 错误信息                     |

<Callout title="Verbose 位掩码">
  你可以通过将值相加来组合级别。例如，`verbose: 3` 将启用 `log`（1）和 `warn`（2）两种日志。
</Callout>

verbose 输出会在返回的数据中（`data` 属性）附带一个名为 `logs` 的属性，`logs` 包含 `info`、`warn` 和 `error` 三个属性，每个都是一个包含对应日志信息的数组。

```js
{
  "data": {
    "content": {
        // Tiptap JSON
    }
  },
  "logs": {
    "info": [],
    "warn": [
      {
        "message": "Image file not found in media files",
        "fileName": "image1.gif",
        "availableMediaFiles": []
      }
    ],
    "error": [
      {
        "message": "Image upload failed: General error",
        "fileName": "image1.gif",
        "url": "https://your-image-upload-endpoint.com",
        "error": "Unable to connect. Is the computer able to access the url?",
        "context": "uploadImage general error"
      }
    ]
  }
}
```

## 支持 & 限制
将 `.docx` 文件导入 Tiptap 提供了一种处理大多数标准 Word 内容的方法，但 **这并不是一对一的映射**，因为 DOCX 格式和 Tiptap 基于 CSS 的样式之间存在固有差异。

当前支持的功能和已知限制：

| **功能**                      | **支持**                                                                |
|----------------------------------|----------------------------------------------------------------------------|
| **文本内容**                 | ✓ 基本文本、间距、标点符号                                         |
| **文本格式化**              | ✓ 粗体、斜体、下划线、删除线、对齐、行高           |
| **块元素**               | ✓ 段落、标题（1–6级）、引用、序号与无序列表     |
| **表格**                       | ✓ 基本结构、表头行、跨列                                    |
| **链接**                        | ✓ 超链接                                                               |
| **媒体（图像）**               | ✓ 嵌入图像，保留尺寸                                          |
| **样式**                       | ✓ 字体系列*、字体颜色、字体大小、背景颜色、行高 |
| **页眉和页脚**            | ✓ 支持（需要[页面扩展](/editor/extensions/functionality/pages)）                            |
| **节与分页符**       | ~ 开发中                                                           |
| **脚注和尾注**         | ✓ 支持                                                                |
| **数学公式**                         | ~ 开发中                                                           |
| **注释与修订**         | ✗                                                                          |
| **目录**            | ✗                                                                          |
| **高级格式化**          | ✗ 多栏、文本方向、表单、宏、嵌入脚本                 |
| **元数据**                     | ✗                                                                          |
| **文本框、形状、SmartArt** | ✗                                                                          |

<span style={{fontSize: '1rem'}}>* 字体系列在打开 `.docx` 文件时，目标字体安装在操作系统上时会被支持。</span>
