---
title: 在选定文本旁边添加浮动工具栏
meta:
  title: 浮动工具栏 | Tiptap UI 组件
  description: 在 Tiptap 编辑器中选择的文本旁边添加浮动工具栏。更多内容请查看文档！
  category: UI 组件
---

一个在文本被选中时出现的浮动工具栏。

## 安装

```bash
npx @tiptap/cli add toolbar-floating
```

## 用法

```tsx
import { ToolbarFloating } from '@/components/tiptap-ui/toolbar-floating'
import { useEditor, EditorContent } from '@tiptap/react'

export default function MyEditor() {
  const editor = useEditor({
    // 编辑器配置
  })

  return (
    <div>
      <EditorContent editor={editor} />
      <ToolbarFloating editor={editor} />
    </div>
  )
}
```

## 属性

| 名称   | 类型           | 默认值 | 描述                        |
| ------ | -------------- | ------- | -------------------------- |
| editor | Editor \| null | null    | Tiptap 编辑器实例         |

## 要求

使用的参考组件

- `app-context` (上下文)
- `use-mobile` (钩子)
- `use-tiptap-editor` (钩子)
- `use-window-size` (钩子)
- `tiptap-utils` (库)
- `comment-button` (UI)
- `floating-element` (UI)
- `heading-dropdown-menu` (UI)
- `list-dropdown-menu` (UI)
- `mark-button` (UI)
- `link-popover` (UI)
- `toolbar` (基本组件)