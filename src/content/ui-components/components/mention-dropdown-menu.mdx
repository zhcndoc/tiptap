---
title: 提及用户的下拉菜单
meta:
  title: 提及下拉菜单 | Tiptap UI 组件
  description: 在您的 Tiptap 编辑器中使用下拉菜单 UI 组件提及其他用户。更多信息请参考我们的文档！
  category: UI 组件
---

## 安装

```bash
npx @tiptap/cli add mention-dropdown-menu
```

## 使用方法

```tsx
import { MentionDropdownMenu } from '@/components/tiptap-ui/mention-dropdown-menu'
import { useEditor, EditorContent } from '@tiptap/react'
import { Mention } from '@tiptap/extension-mention'

export default function MyEditor() {
  const editor = useEditor({
    extensions: [
      // 其他扩展
      Mention.configure({
        // 提及配置
      }),
    ],
  })

  return (
    <div>
      <EditorContent editor={editor} />
      <MentionDropdownMenu editor={editor} />
    </div>
  )
}
```

## 组件属性

| 名称             | 类型             | 默认值                      | 描述                     |
| ---------------- | ---------------- | --------------------------- | ------------------------ |
| editor           | Editor \| null   | null                        | Tiptap 编辑器实例      |
| floatingOptions  | object           | -                           | 浮动UI的选项            |
| selector         | string           | 'tiptap-mention-dropdown-menu' | 目标选择器             |
| char             | string           | '@'                         | 提及字符                |

## 依赖组件

- `tiptap-utils`（库）
- `suggestion-menu`（UI）
- `avatar`（原语）
- `button`（原语）