---
title: 添加一个表情符号下拉菜单
meta:
  title: 表情符号下拉 | Tiptap UI 组件
  description: 添加一个下拉菜单，将表情符号插入到 Tiptap 编辑器中。更多请查看文档！
  category: UI 组件
---

## 安装

```bash
npx @tiptap/cli add emoji-dropdown-menu
```

## 使用

```tsx
import { EmojiDropdownMenu } from '@/components/tiptap-ui/emoji-dropdown-menu'
import { useEditor, EditorContent } from '@tiptap/react'
import { Emoji } from '@tiptap-pro/extension-emoji'

export default function MyEditor() {
  const editor = useEditor({
    extensions: [
      // 其他扩展
      Emoji.configure({
        // 表情符号配置
      }),
    ],
  })

  return (
    <div>
      <EditorContent editor={editor} />
      <EmojiDropdownMenu editor={editor} />
    </div>
  )
}
```

## 属性

| 名称            | 类型           | 默认值                       | 描述                      |
| --------------- | -------------- | ---------------------------- | ------------------------- |
| editor          | Editor \| null | null                         | Tiptap 编辑器实例        |
| floatingOptions | object         | -                            | 浮动 UI 的选项           |
| selector        | string         | 'tiptap-emoji-dropdown-menu' | 目标选择器               |
| char            | string         | ':'                          | 表情符号字符             |

## 需求

使用的参考组件

- `suggestion-menu` (ui)
- `emoji-menu` (ui)

开源功能

- `@tiptap/react`

专业功能

- `@tiptap-pro/extension-emoji`