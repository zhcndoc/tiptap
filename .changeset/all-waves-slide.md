---
'tiptap-docs': 修订
---

更新列表下拉菜单文档：

- 钩子名称由 `useListState()` 更改为 `useListDropdownMenu()`
- 钩子改为接受对象参数，而非位置参数
- 钩子属性表中将 `availableTypes` 参数重命名为 `types`
- 移除 `listInSchema`
- 将 `canToggleAny` 重命名为 `canToggle`
- 将 `isAnyActive` 重命名为 `isActive`
- 添加 `activeType` 到返回值
- 添加 `types` 到返回值
- 添加 `label` 到返回值
