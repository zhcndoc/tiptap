---
'tiptap-docs': 修订
---

在 `<AiMenu />` 和 `<AiMenuContent />` 属性表中添加了 `anchorToSelection` 属性文档。更新了 `useAiContentTracker` 和 `useTextSelectionTracker` 钩子的文档，包含完整的参数表和新的调用签名。新增实用工具的文档：`getSelectionRangeRect`、`createEditorWidthAnchorRect`、`createVirtualAnchor` 以及 `getEditorContentRect`。
