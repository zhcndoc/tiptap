---
'tiptap-docs': patch
---

更新了浮动元素文档：

- 在属性表中添加了缺失的 `referenceElement` 属性。
- 记录了 `isElementWithinEditor` 工具函数。
- 记录了 `isTextSelectionValid` 工具函数。
- 通过详细的参数和返回类型信息改进了工具函数文档。
- 给所有代码示例添加了完整的导入语句。
- 添加了一个演示 `referenceElement` 用法的新示例。
- 扩展了自定义定位示例，提供了更清晰的实现细节。